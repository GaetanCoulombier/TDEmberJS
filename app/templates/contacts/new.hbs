{{page-title "New"}}
<h2>
  Ajouter un contact
</h2>
<Input type="text" @value={{@model.nom}} />
<Input type="text" @value={{@model.prenom}} />
<Input type="email" @value={{@model.email}} />
<button type="button" {{action "save" @model}}>
  Enregistrer
</button>
{{outlet}}